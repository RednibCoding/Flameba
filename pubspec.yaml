name: flameba
description: Utility and Widgets Package for Flutter
publish_to: 'none'

version: 1.2.0+100000

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_mobx: 2.0.4
  mobx: 2.0.6+1
  intl: ^0.17.0
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

  mobx_codegen: 2.0.5+2
  build_runner: 2.1.10

# flutter: